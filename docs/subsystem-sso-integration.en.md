# External System SSO Integration

Integrate with **moli-admin** (this repo; Maven module `moli-server`).

Assign system access on moli-admin via `PUT /user/insertUserSystem`. Local RBAC stays in each external app.

Chinese: [subsystem-sso-integration.md](subsystem-sso-integration.md)
