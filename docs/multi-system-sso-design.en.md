# Multi-System SSO Design

**moli-admin** is this project. The Maven folder `moli-server` is only a module name — not a separate system.

moli-admin handles: login, local RBAC, system portal, user–system assignment (`insertUserSystem`), and SSO tickets for external apps.

Chinese: [multi-system-sso-design.md](multi-system-sso-design.md)
